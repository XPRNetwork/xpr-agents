name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  # ── SDK ────────────────────────────────────
  sdk:
    name: SDK (Jest)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm install
        working-directory: sdk
      - run: npm run build
        working-directory: sdk
      - run: npm test
        working-directory: sdk

  # ── Contracts ──────────────────────────────
  # All contract tests run in a single job because they have
  # cross-contract WASM dependencies (agentfeed/valid/escrow
  # load agentcore's WASM, agentescrow loads agentfeed's WASM).
  contracts:
    name: Contracts (all)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20

      # Install all contracts
      - run: npm install
        working-directory: contracts/agentcore
      - run: npm install
        working-directory: contracts/agentfeed
      - run: npm install
        working-directory: contracts/agentvalid
      - run: npm install
        working-directory: contracts/agentescrow

      # Build in dependency order: core → feed → valid/escrow
      - name: Build agentcore
        run: npm run build
        working-directory: contracts/agentcore
      - name: Build agentfeed
        run: npm run build
        working-directory: contracts/agentfeed
      - name: Build agentvalid
        run: npm run build
        working-directory: contracts/agentvalid
      - name: Build agentescrow
        run: npm run build
        working-directory: contracts/agentescrow

      # Test in dependency order
      - name: Test agentcore (71 tests)
        run: npm test
        working-directory: contracts/agentcore
      - name: Test agentfeed (16 tests)
        run: npm test
        working-directory: contracts/agentfeed
      - name: Test agentvalid (15 tests)
        run: npm test
        working-directory: contracts/agentvalid
      - name: Test agentescrow (23 tests)
        run: npm test
        working-directory: contracts/agentescrow

  # ── OpenClaw Plugin ────────────────────────
  openclaw:
    name: OpenClaw (Vitest)
    runs-on: ubuntu-latest
    needs: sdk
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      # Build SDK first (openclaw depends on it)
      - run: npm install && npm run build
        working-directory: sdk
      # Override SDK dep to use local build
      - name: Link local SDK
        run: |
          npm install
          npm rm @xpr-agents/sdk 2>/dev/null || true
          npm install ../sdk
        working-directory: openclaw
      - run: npm run build
        working-directory: openclaw
      - run: npm test
        working-directory: openclaw

  # ── Indexer ────────────────────────────────
  indexer:
    name: Indexer (Vitest)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm install
        working-directory: indexer
      - run: npm run build
        working-directory: indexer
      - run: npm test
        working-directory: indexer

  # ── Frontend ───────────────────────────────
  frontend:
    name: Frontend (Next.js)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm install
        working-directory: frontend
      - run: npm run build
        working-directory: frontend

  # ── Agent Runner ───────────────────────────
  agent-runner:
    name: Agent Runner (Build)
    runs-on: ubuntu-latest
    needs: openclaw
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm install && npm run build
        working-directory: sdk
      - name: Build openclaw with local SDK
        run: |
          npm install
          npm rm @xpr-agents/sdk 2>/dev/null || true
          npm install ../sdk
          npm run build
        working-directory: openclaw
      - run: npm install && npm run build
        working-directory: openclaw/starter/agent

  # ── Scaffold sanity ────────────────────────
  # Prevents regressions where the create-xpr-agent template diverges from
  # the openclaw/starter source, and ensures the templates never ship the
  # legacy `XPR_PRIVATE_KEY` / `--key` patterns that the v0.3.0+ agent
  # runner refuses to boot with.
  scaffold:
    name: Scaffold Sanity
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Template ↔ source scripts in sync
        run: |
          set -e
          # The standalone scaffold ships start.sh only. setup.sh (Docker) was
          # removed when Docker was demoted to legacy under openclaw/starter/docker/.
          diff create-xpr-agent/template/start.sh openclaw/starter/start.sh \
            || { echo "::error::create-xpr-agent/template/start.sh has drifted from openclaw/starter/start.sh. Run: cp openclaw/starter/start.sh create-xpr-agent/template/start.sh"; exit 1; }
          diff create-xpr-agent/template/.env.example openclaw/starter/.env.example \
            || { echo "::error::create-xpr-agent/template/.env.example has drifted from openclaw/starter/.env.example. Run: cp openclaw/starter/.env.example create-xpr-agent/template/.env.example"; exit 1; }
          diff create-xpr-agent/template/setup-security.sh openclaw/starter/setup-security.sh \
            || { echo "::error::create-xpr-agent/template/setup-security.sh has drifted from openclaw/starter/setup-security.sh. Run: cp openclaw/starter/setup-security.sh create-xpr-agent/template/setup-security.sh"; exit 1; }
          diff openclaw/scripts/setup-security.sh openclaw/starter/setup-security.sh \
            || { echo "::error::openclaw/scripts/setup-security.sh has drifted from openclaw/starter/setup-security.sh. Run: cp openclaw/starter/setup-security.sh openclaw/scripts/setup-security.sh"; exit 1; }
          if [ -f create-xpr-agent/template/setup.sh ]; then
            echo "::error::create-xpr-agent/template/setup.sh has been re-introduced. Docker is legacy under openclaw/starter/docker/ — remove the template copy."
            exit 1
          fi
      - name: No legacy XPR_PRIVATE_KEY in shipped scripts
        run: |
          set -e
          # Either set/exported as a runtime variable — both are unsupported
          # under the v0.3.0+ keychain pattern. (Allow it to appear as a
          # rejection / migration message — those tell the user what to do.)
          FILES=()
          for f in create-xpr-agent/template/start.sh openclaw/starter/start.sh openclaw/starter/bootstrap.sh; do
            [ -f "$f" ] && FILES+=("$f")
          done
          BAD=$(grep -nE '^[[:space:]]*(export[[:space:]]+)?XPR_PRIVATE_KEY=' "${FILES[@]}" 2>/dev/null || true)
          if [ -n "$BAD" ]; then
            echo "::error::Scripts assign or export XPR_PRIVATE_KEY at runtime — incompatible with the proton CLI keychain pattern:"
            echo "$BAD"
            exit 1
          fi
      - name: No --key flag plumbing in shipped scripts
        run: |
          set -e
          # The --key flag must either not exist OR exist solely to reject
          # the legacy invocation with a migration message. Accept lines
          # like `--key)` only if followed by an `err`/`exit 1` block within
          # 10 lines.
          for f in create-xpr-agent/template/start.sh openclaw/starter/start.sh; do
            [ -f "$f" ] || continue
            if grep -nE '^[[:space:]]*--key[)]?[[:space:]]+XPR_PRIVATE_KEY=' "$f" >/dev/null; then
              echo "::error::$f still accepts --key into XPR_PRIVATE_KEY (legacy pattern)"
              exit 1
            fi
          done
      - name: No --key in user-facing banners / docs / examples
        run: |
          set -e
          # create.mjs prints a next-steps banner after scaffolding — that text
          # ships to operators every time they run npx create-xpr-agent. It
          # also has to match the keychain-only model. Same for any
          # documentation-as-example commands.
          FILES=()
          for f in create-xpr-agent/create.mjs create-xpr-agent/README.md create-xpr-agent/template/README.md create-xpr-agent/template/QUICKSTART.md openclaw/starter/README.md docs/PINATA.md; do
            [ -f "$f" ] && FILES+=("$f")
          done
          BAD=$(grep -RnE -- '--key[[:space:]]+(YOUR_KEY|PVT_K1_|\$|<)' "${FILES[@]}" 2>/dev/null || true)
          if [ -n "$BAD" ]; then
            echo "::error::Found --key references in user-facing banners or docs (must use proton CLI keychain instead):"
            echo "$BAD"
            exit 1
          fi
