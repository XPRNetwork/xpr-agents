# Deploy backend URL
NEXT_PUBLIC_DEPLOY_API=http://localhost:3500

# Network (testnet or mainnet)
NEXT_PUBLIC_NETWORK=testnet

# Request account for wallet (must exist on-chain)
NEXT_PUBLIC_REQUEST_ACCOUNT=agentcore
