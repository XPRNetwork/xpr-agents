{
  "name": "agentvalid",
  "version": "1.0.0",
  "description": "XPR Network Agent Validation Registry Contract",
  "main": "assembly/agentvalid.contract.ts",
  "scripts": {
    "build": "proton-tsc assembly/agentvalid.contract.ts",
    "test": "proton-vert tests/*.test.ts"
  },
  "dependencies": {
    "proton-tsc": "^0.9.0"
  },
  "devDependencies": {
    "@proton/vert": "^0.2.0"
  }
}
